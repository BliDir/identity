# Benchmarks

Place benchmark targets here when performance-sensitive code is added.

For stable Rust projects, consider keeping benchmarks as explicit binaries or
using a dedicated benchmark crate such as Criterion when needed.
